const table_heads = document.querySelectorAll('.t-head');
const add_user_form = document.getElementById('form_addUser');
const find_users_form = document.getElementById('form_findUsers');
const url = 'http://localhost:3000/api/students';